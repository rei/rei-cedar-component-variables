{
  "name": "cdr-radio",
  "notes": [
    "styles/svg for the radio input are not currently exported",
    "cdr-radio and cdr-checkbox both make use of the cdr-label mixins",
    "examples assume that the <input/> element is inside of the <label>"
  ],
  "docs": "https://rei.github.io/rei-cedar-docs/components/radio/",
  "examples": [
    {
      "name": "default",
      "tag": "label",
      "styles": ["@include cdr-label-base-mixin;", "& input { margin-right: $cdr-label-base-spacing; }"],
      "preContent": "<input type=\"radio\"/>",
      "attrs": {
      }
    },
    {
      "name": "compact",
      "tag": "label",
      "styles": ["@include cdr-label-base-mixin;", "@include cdr-label-compact-mixin;", "& input { margin-right: $cdr-label-base-spacing; }"],
      "preContent": "<input type=\"radio\"/>",
      "attrs": {
        "modifier": "compact"
      }
    }
  ]
}
